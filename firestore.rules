rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /lifePlans/{planId} {
      allow read: if true;
      allow create: if true; // Allow anyone to create for now
    }
  }
}
